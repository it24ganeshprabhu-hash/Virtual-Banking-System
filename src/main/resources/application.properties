spring.application.name=VirtualBankingSystem
server.port=10000

# ==========================
# MySQL Database Configuration (default)
# (Commented out to use PostgreSQL)
# ==========================
# spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASS}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ==========================
# PostgreSQL Database Configuration (active)
# ==========================
spring.datasource.url=jdbc:postgresql://dpg-d3s5iigdl3ps73d40qug-a.oregon-postgres.render.com:5432/vbs_db
spring.datasource.username=vbs_db_user
spring.datasource.password=7nYJatnGO5XZxzQvcVokxM9kbXzCdeox
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================
# JPA/Hibernate Configuration
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==========================
# Logging Configuration
# ==========================
logging.level.com.vbs.VirtualBankingSystem=DEBUG
logging.level.org.springframework.web=DEBUG

# ==========================
# JWT Configuration
# ==========================
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}
